services:
  - type: web
    name: domain-search
    env: node
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: REDIS_URL
        sync: false
